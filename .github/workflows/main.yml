naname: First workflows

on:
  push:
    branches:
      - main

jobs:
  exemples-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: print welcome message
        run: echo "welcome to github"

   
   
