name: First workflows
on: 
   push:
      branches: 
         - main
jobs:
   exemples-jobs:
     runs-on: ubuntu:latest
   steps:
     -name: print welcom message
       run: echo "welcom to github"

   
   
